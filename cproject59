#define  _CRT_SECURE_NO_WARNINGS 
#include<stdio.h>

int main()
{
	int a[100],i=0,n=0;
	scanf("%d", &n);
	for (i=0; i <n; i++)
	{
		scanf("%d", &a[i]);
	}
	for ( i = 0; i < n; i++)
	{
		printf("%d\n", (a[i]?1:0));
	}
	return 0;
}
